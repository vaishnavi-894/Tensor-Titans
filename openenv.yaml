name: support-ticket-triage-openenv
version: 0.1.0
description: Real-world customer support ticket triage environment for RL and agent evaluation.
entrypoint: app.main:app
api:
  reset: POST /reset
  step: POST /step
  state: GET /state
  tasks: GET /tasks
  grader: GET /grader
  baseline: GET /baseline
  health: GET /health
tasks:
  - easy_refund
  - medium_bug
  - hard_security